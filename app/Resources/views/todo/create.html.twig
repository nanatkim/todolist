{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-default" href="/todo">Back</a>
    <hr>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <div class="panel panel-default">
            <div class="panel-heading">New Todo</div>
            <div class="panel-body">
                {{form_start(form)}}
                {{form_widget(form)}}
                {{form_end(form)}}
            </div>
        </div>
    </div>
</div>

{% endblock %}