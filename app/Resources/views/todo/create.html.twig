{% extends 'base.html.twig' %}

{% block body %}
<div class="col-md-3">
    <a class="btn btn-default" href="/todo">Back</a>
</div>
<div class="col-md-6">
    <h2 class="page-header">Add Todo</h2>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
</div>

{% endblock %}