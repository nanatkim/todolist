{% extends 'base.html.twig' %}

{% block body %}
<div class="col-md-4">
    <a class="btn btn-default" href="/user">Back</a>
</div>
<div class="col-md-4">
    <h2 class="page-header">Editar Perfil</h2>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
</div>

{% endblock %}