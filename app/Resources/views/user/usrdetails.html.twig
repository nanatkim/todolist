{% extends 'base.html.twig' %} {% block body %}
<a class="btn btn-default" href="/user">Back</a>
<hr>
<div class="col-md-12">
    <h2 class="page-header">{{user.name}}</h2>
    <div class="col-md-4">
        <img src="https://i.pinimg.com/736x/8e/05/89/8e058925f17ed44596fada7cdeb3f378--taeyeon-short-hair-girls-generation.jpg" class="img-circle" width="250" height="250">
    </div>
    <div class="col-md-6">
        <ul class="list-group">
            <li class="list-group-item">Email:
            </li>
            <li class="list-group-item">{{user.email}}</li>
            <hr>
            {% if (user.aboutme == null) and (app.user != null) and (app.user.id == user.id) %}
                <li class="list-group-item active">
                    <a class="btn btn-primary" href="/description/{{user.id}}">Adicionar descrição</a>
                </li>
            {% elseif user.aboutme != null %}
                <li class="list-group-item">About Me: {{user.aboutme}}</li>
            {% endif %}
        </ul>
    </div>
    <div class="col-md-1 col-md-offset-1">
        <a class="btn btn-default">Edit</a>
    </div>
</div>
{% endblock %}